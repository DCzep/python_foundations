program_words = {
    'variables': 'labels that you can assign to values',
    'strings': 'a series of characters',
    'whitespace': 'any nonprinting characters',
    'floats': 'any number with a decimal',
    'constant': 'a variables whose value stays the same throughout the life of a program',
}

print("Variables:", program_words['variables'].title())
print("Strings:", program_words['strings'].title())
print("Whitespace:", program_words['whitespace'].title())
print("Floats:", program_words['floats'].title())
print("Constant:", program_words['constant'].title())

print("Variables:\n", program_words['variables'].title())
print("Strings:\n", program_words['strings'].title())
print("Whitespace:\n", program_words['whitespace'].title())
print("Floats:\n", program_words['floats'].title())
print("Constant:\n", program_words['constant'].title())
